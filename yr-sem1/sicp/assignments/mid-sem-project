#lang scheme
(define (make-point x y)
  (cons x y))
(define (point-x p)
  (car p)
 (define set-x!( set! x p))

(define (point-y p)
(cdr p))
(define (pretty-print p)
(list 'x: (point-x p) 'y: (point-y p)))

(pretty-print (make-point 2 3))



;solution
