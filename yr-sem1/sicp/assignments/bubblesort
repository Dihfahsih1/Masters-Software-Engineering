#lang scheme

;QTN ONE
;Write a program bubblesort that takes a list of numbers and returns the
;list in sorted order. For example:
;% (bubblesort (quote (1 4 2 8 5 7)))
;(1 2 4 5 7 8)

(define (bubble L)
 (if (null? (cdr L))
  L
  (if (< (car L) (cadr L))
      (cons(car L) (bubble (cdr L)))
      (cons(cadr L) (bubble (cons (car L) (cddr L)))))))

(define (bubble-sort N L)
  (cond (( = N 1 ) (bubble L))
        (else (bubble-sort ( - N 1 ) (bubble L)))))
  
(bubble-sort 6 (list 132 4 2 8 5 7))