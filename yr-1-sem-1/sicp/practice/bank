
#lang scheme


(define withdraw
  (let ((balance 1000))
    (lambda (amount)
  (if (>= balance amount)
      (begin (set! balance (- balance amount))
             balance)
      "Insufficient funds"))))

(withdraw 100)

(define money 10)
(define adding 
 (lambda *(withdraw money))
)
  
(adding)